# 테이블오더 서비스 - 요구사항 문서

## 1. Intent Analysis

| 항목 | 내용 |
|------|------|
| **사용자 요청** | 테이블오더 서비스 구축 (고객 주문 + 관리자 운영) |
| **요청 유형** | New Project (Greenfield) |
| **범위** | System-wide (고객 UI, 관리자 UI, API, 데이터베이스) |
| **복잡도** | Complex (실시간 통신, 인증, 세션 관리, 다수 기능) |

---

## 2. 기술 스택 결정

| 영역 | 기술 | 비고 |
|------|------|------|
| **프레임워크** | Next.js (TypeScript) | 풀스택 - API Routes로 백엔드 처리 |
| **프론트엔드** | React (Next.js 내장) | TypeScript 기반 |
| **백엔드 API** | Next.js Route Handlers | App Router 기반 |
| **데이터베이스** | SQLite | 경량 DB, 단일 매장용 적합 |
| **ORM** | Prisma 또는 Drizzle (TBD) | SQLite 호환 ORM |
| **실시간 통신** | Server-Sent Events (SSE) | 주문 실시간 업데이트 |
| **인증** | JWT 토큰 기반 | 관리자 16시간 세션, 테이블 자동 로그인 |
| **배포** | 미정 | 개발 우선, 배포는 추후 결정 |

---

## 3. 아키텍처 결정

| 항목 | 결정 |
|------|------|
| **아키텍처 패턴** | 풀스택 모놀리식 (Next.js 단일 프로젝트) |
| **매장 지원** | 단일 매장 (Single-tenant) |
| **인터페이스 구성** | 하나의 앱에서 URL 경로로 구분 (/customer, /admin) |
| **이미지 관리** | 외부 이미지 URL 직접 입력 |
| **관리자 계정** | 시드 데이터로 1개 계정 생성 |
| **테스트 전략** | 핵심 비즈니스 로직 단위 테스트 |
| **보안 규칙** | 모든 SECURITY 규칙 적용 (blocking constraint) |

---

## 4. 기능 요구사항 (Functional Requirements)

### 4.1 고객용 기능 (Customer Features)

#### FR-C01: 테이블 태블릿 자동 로그인 및 세션 관리
- **초기 설정 (관리자 1회 수행)**: 매장 식별자, 테이블 번호, 테이블 비밀번호 입력 후 로컬 저장
- **자동 로그인**: 저장된 정보로 자동 인증, 별도 로그인 절차 없이 즉시 주문 가능
- **세션 관리**: 테이블 세션은 첫 주문 시작부터 이용 완료 처리까지 유지

#### FR-C02: 메뉴 조회 및 탐색
- 메뉴 화면이 기본 화면으로 항상 표시
- 카테고리별 메뉴 분류 및 표시
- 메뉴 상세 정보: 메뉴명, 가격, 설명, 이미지(외부 URL)
- 카테고리 간 빠른 이동
- 카드 형태 레이아웃, 터치 친화적 버튼 (최소 44x44px)

#### FR-C03: 장바구니 관리
- 메뉴 추가/삭제, 수량 조절 (증가/감소)
- 총 금액 실시간 계산
- 장바구니 비우기
- 클라이언트 측 로컬 저장 (페이지 새로고침 시에도 유지)
- 서버 전송은 주문 확정 시에만 수행

#### FR-C04: 주문 생성
- 주문 내역 최종 확인 후 주문 확정
- 주문 성공 시: 주문 번호 표시 → 장바구니 자동 비우기 → 메뉴 화면으로 자동 리다이렉트
- 주문 실패 시: 에러 메시지 표시, 장바구니 유지
- 주문 정보: 매장 식별 정보, 테이블 식별 정보, 주문 메뉴 목록(메뉴명, 수량, 단가), 총 주문 금액, 세션 ID

#### FR-C05: 주문 내역 조회
- 현재 테이블 세션 주문만 표시 (이전 세션 제외)
- 주문 시간 순 정렬
- 주문별 상세: 주문 번호, 주문 시각, 주문 메뉴 및 수량, 주문 금액, 주문 상태(대기중/준비중/완료)
- 매장 이용 완료 처리된 주문은 제외

### 4.2 관리자용 기능 (Admin Features)

#### FR-A01: 매장 인증
- 매장 식별자, 사용자명, 비밀번호 입력
- JWT 토큰 기반 인증, 16시간 세션 유지
- 브라우저 새로고침 시 세션 유지, 16시간 후 자동 로그아웃
- 비밀번호 bcrypt 해싱, 로그인 시도 제한

#### FR-A02: 실시간 주문 모니터링
- SSE 기반 실시간 주문 목록 업데이트 (2초 이내 표시)
- 그리드/대시보드 레이아웃: 테이블별 카드 형태 표시
- 각 테이블 카드: 총 주문액, 최신 주문 미리보기
- 주문 카드 클릭 시 전체 메뉴 목록 상세 보기
- 주문 상태 변경 (대기중 → 준비중 → 완료)
- 신규 주문 시각적 강조 (색상 변경, 애니메이션)
- 테이블별 필터링 기능

#### FR-A03: 테이블 관리
- **테이블 초기 설정**: 테이블 번호 및 비밀번호 설정, 16시간 세션 생성
- **주문 삭제**: 특정 주문 삭제 (확인 팝업), 테이블 총 주문액 재계산
- **테이블 세션 처리**: 이용 완료 시 주문 내역을 과거 이력으로 이동, 현재 주문 목록 및 총 주문액 0으로 리셋
- **과거 주문 내역 조회**: 테이블별 과거 주문 목록 (시간 역순), 날짜 필터링

#### FR-A04: 메뉴 관리
- 메뉴 CRUD (등록/조회/수정/삭제)
- 메뉴 정보: 메뉴명, 가격, 설명, 카테고리, 이미지 URL
- 카테고리별 조회
- 메뉴 노출 순서 조정
- 필수 필드 검증, 가격 범위 검증

---

## 5. 비기능 요구사항 (Non-Functional Requirements)

### NFR-01: 성능
- SSE 기반 실시간 주문 알림 2초 이내 전달
- 메뉴 조회 응답 시간 1초 이내
- 동시 접속 테이블 수: 단일 매장 기준 최소 20개 테이블 지원

### NFR-02: 보안
- JWT 토큰 기반 인증 (관리자 16시간, 테이블 자동 로그인)
- 비밀번호 bcrypt 해싱
- 로그인 시도 제한 (brute-force 방지)
- 입력값 검증 및 SQL Injection 방지
- SECURITY 확장 규칙 전체 적용 (SECURITY-01 ~ SECURITY-15)

### NFR-03: 사용성
- 터치 친화적 UI (최소 44x44px 버튼)
- 직관적인 카드 기반 레이아웃
- 명확한 시각적 피드백 (성공/실패)
- 반응형 디자인 (태블릿 최적화)

### NFR-04: 신뢰성
- 장바구니 로컬 저장 (페이지 새로고침 시 유지)
- 주문 실패 시 장바구니 데이터 보존
- 에러 발생 시 사용자 친화적 메시지 표시

### NFR-05: 유지보수성
- TypeScript 기반 타입 안전성
- 모듈화된 코드 구조
- 핵심 비즈니스 로직 단위 테스트

---

## 6. 제외 범위 (Out of Scope)

requirements/constraints.md에 정의된 제외 기능:
- 결제 처리 (PG사 연동, 영수증, 환불, 포인트/쿠폰)
- 복잡한 인증 (OAuth, SNS 로그인, 2FA)
- 파일/컨텐츠 관리 (이미지 리사이징, CMS, 광고)
- 알림 시스템 (푸시, SMS, 이메일, 소리/진동)
- 주방 기능 (주방 전달, 재고 관리)
- 고급 기능 (데이터 분석, 매출 리포트, 직원 관리, 예약, 리뷰, 다국어)
- 외부 연동 (배달 플랫폼, POS, 소셜 미디어, 지도/번역 API)

---

## 7. 데이터 모델 개요

### 핵심 엔티티
- **Store**: 매장 정보 (단일 매장)
- **Admin**: 관리자 계정 (시드 데이터 1개)
- **Table**: 테이블 정보 (번호, 비밀번호, 세션 정보)
- **Category**: 메뉴 카테고리
- **MenuItem**: 메뉴 항목 (이름, 가격, 설명, 이미지 URL, 카테고리, 노출 순서)
- **Order**: 주문 (테이블, 세션 ID, 상태, 총 금액, 주문 시각)
- **OrderItem**: 주문 항목 (메뉴, 수량, 단가)
- **OrderHistory**: 과거 주문 이력 (이용 완료 처리된 주문)
- **TableSession**: 테이블 세션 (시작 시각, 종료 시각, 세션 ID)

---

## 8. API 엔드포인트 개요

### 고객용 API
- `POST /api/tables/login` — 테이블 자동 로그인
- `GET /api/menu` — 메뉴 목록 조회 (카테고리별)
- `GET /api/menu/:id` — 메뉴 상세 조회
- `POST /api/orders` — 주문 생성
- `GET /api/orders` — 현재 세션 주문 내역 조회

### 관리자용 API
- `POST /api/admin/login` — 관리자 로그인
- `GET /api/admin/orders/stream` — SSE 실시간 주문 스트림
- `PATCH /api/admin/orders/:id/status` — 주문 상태 변경
- `DELETE /api/admin/orders/:id` — 주문 삭제
- `POST /api/admin/tables/:id/complete` — 테이블 이용 완료
- `GET /api/admin/tables/:id/history` — 과거 주문 내역 조회
- `POST /api/admin/tables` — 테이블 초기 설정
- `GET /api/admin/menu` — 메뉴 관리 조회
- `POST /api/admin/menu` — 메뉴 등록
- `PUT /api/admin/menu/:id` — 메뉴 수정
- `DELETE /api/admin/menu/:id` — 메뉴 삭제
- `PATCH /api/admin/menu/order` — 메뉴 노출 순서 변경

---

## 9. 프로젝트 구조 개요

```
table-order/
├── src/
│   └── app/
│       ├── (customer)/          # 고객용 페이지
│       │   ├── menu/            # 메뉴 조회
│       │   ├── cart/            # 장바구니
│       │   └── orders/          # 주문 내역
│       ├── (admin)/             # 관리자용 페이지
│       │   ├── login/           # 관리자 로그인
│       │   ├── dashboard/       # 주문 모니터링
│       │   ├── tables/          # 테이블 관리
│       │   └── menu-manage/     # 메뉴 관리
│       └── api/                 # API Route Handlers
│           ├── tables/
│           ├── menu/
│           ├── orders/
│           └── admin/
├── prisma/                      # DB 스키마 및 마이그레이션
├── lib/                         # 공통 유틸리티
├── components/                  # 공유 UI 컴포넌트
└── types/                       # TypeScript 타입 정의
```
